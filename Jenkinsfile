pipeline {
    agent any
    stages {
        stage('Build') {  // Use 'stage' (singular) here, not 'stages'
            steps {
                sh 'echo "Build Completed."'
            }
        }
    }
}
